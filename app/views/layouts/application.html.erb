<!DOCTYPE html>
<html>
<head>
  <title>Blocitoff</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav>
  <ul>
    <li><%= link_to("Register", :new_user_registration)%></li>
    <li><%= current_user ? link_to("Sign Out", :destroy_user_session, method: "DELETE") : link_to("Sign In", :new_user_session) %></li>
    
  </ul>
</nav>
  <p><%= flash[:notice] if flash[:notice] %></p>
  <p><%= flash[:alert] if flash[:alert] %></p>

<%= yield %>

</body>
</html>
